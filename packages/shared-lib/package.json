{
  "name": "@RoomParty/shared-lib",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc",
    "lint": "eslint *.ts*",
    "dev": "yarn clean && tsc-watch --noClear",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "@types/node": "^17.0.12",
    "@RoomParty/tsconfig": "*",
    "typescript": "^4.8.3",
    "tsc-watch": "^5.0.3"
  }
}
