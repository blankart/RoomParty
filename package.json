{
  "name": "tube-hub-monorepo",
  "version": "3.22.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "turbo": "turbo",
    "dev:turbo": "env-cmd --file .env.local turbo",
    "prepare": "husky install",
    "build": "turbo run build",
    "build:server": "turbo run build --filter=@rooms2watch/server --",
    "start:server": "turbo run start --filter=@rooms2watch/server --",
    "start:web": "turbo run start --filter=@rooms2watch/web --",
    "start": "turbo run start --parallel",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test:unit": "turbo run test:unit --color",
    "format": "prettier --write \"**/*.{ts,tsx,md,js}\"",
    "dev:build": "env-cmd --file .env.local turbo run dev:build",
    "dev:start": "env-cmd --file .env.local turbo run start",
    "dev": "env-cmd --file .env.local turbo run dev --parallel",
    "dev:only": "env-cmd --file .env.local turbo run dev:only --parallel",
    "dev:migrate": "env-cmd --file .env.local turbo run migrate --filter=@rooms2watch/prisma-client --",
    "dev:reset": "env-cmd --file .env.local turbo run reset --filter=@rooms2watch/prisma-client --",
    "dev:deploy": "env-cmd --file .env.local turbo run deploy --filter=@rooms2watch/prisma-client --",
    "dev:generate": "env-cmd --file .env.local turbo run generate --filter=@rooms2watch/prisma-client --",
    "dev:pull": "env-cmd --file .env.local turbo run pull --filter=@rooms2watch/prisma-client --",
    "migrate": "env-cmd --file .env.production turbo run migrate --filter=@rooms2watch/prisma-client --",
    "reset": "env-cmd --file .env.production turbo run reset --filter=@rooms2watch/prisma-client --",
    "deploy": "env-cmd --file .env.production turbo run deploy --filter=@rooms2watch/prisma-client --",
    "pull": "env-cmd --file .env.production turbo run pull --filter=@rooms2watch/prisma-client --",
    "prisma:deploy": "env-cmd --file .env.production turbo run deploy --filter=@rooms2watch/prisma-client --",
    "generate": "env-cmd --file .env.production turbo run generate --filter=@rooms2watch/prisma-client --",
    "gcloud:deploy:server": "./scripts/server/gcloud-deploy-server.sh",
    "gcloud:build:server": "gcloud builds submit",
    "dev:docker:start": "docker-compose --env-file .env.local -f docker-compose.yml up -d",
    "docker:start": "docker-compose --env-file .env.production -f docker-compose.yml up -d",
    "dev:docker:build-and-push": "./scripts/server/build-and-push.sh .env.production",
    "docker:build-and-push": "./scripts/server/build-and-push.sh .env.production",
    "docker:build-push-and-deploy": "./scripts/server/build-push-and-deploy.sh .env.production"
  },
  "devDependencies": {
    "@rooms2watch/eslint-config-custom": "*",
    "env-cmd": "^10.1.0",
    "husky": "^8.0.1",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.19"
}
